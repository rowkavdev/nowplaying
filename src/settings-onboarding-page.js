// First-run browser UI and server-management panel for installed users.
// Same endpoints, credentials and sign-in UI in both states: no wizard window.
export const ONBOARDING_HTML = `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>NowPlaying settings</title><link rel="stylesheet" href="/status.css"><link rel="stylesheet" href="/settings.css"></head><body><main><nav><span aria-current="page">Settings</span></nav><h1>Set up NowPlaying</h1><p>Connect a media server here. NowPlaying runs on this computer and keeps your sign-in in the operating system's credential store.</p>${serverPanel()}${servicePanel()}<p id="first-run-state" role="status">Once a server is added, NowPlaying will start. Keep this page open.</p><p id="activation-recovery" hidden><a href="/settings">Reload setup</a> or restart NowPlaying, then check your server settings.</p></main><script src="/servers.js"></script></body></html>`;
export function serverPanel() { return `<section id="servers-section" aria-labelledby="h-servers"><h2 id="h-servers">Media servers</h2><ul id="servers-list" class="plain"><li>Loading...</li></ul><p>Quick scan checks this PC and sends a Jellyfin/Emby announcement probe. Depending on your network routes, that broadcast may also reach a VPN. To check all hosts on a subnet, enter its private IPv4 address below. A VPN may use a private address too, so check which network you choose.</p><div class="row"><label for="scan-subnet">Subnet to scan (optional)</label><input id="scan-subnet" type="text" placeholder="192.168.1.0/24" autocomplete="off"><button type="button" id="discover-servers">Find servers</button><button type="button" id="cancel-discovery" hidden>Cancel scan</button><span id="discovery-state" role="status" aria-live="polite"></span></div><ul id="discovered-list" class="plain"></ul><details id="manual-add"><summary>Add a server by address</summary><div class="row"><label for="server-provider">Server type</label><select id="server-provider"><option value="plex">Plex</option><option value="jellyfin">Jellyfin</option><option value="emby">Emby</option><option value="navidrome">Navidrome</option></select></div><div class="row"><label for="server-url">Address</label><input type="url" id="server-url" placeholder="http://192.168.1.20:8096" autocomplete="off"></div><button type="button" id="manual-connect">Connect</button></details><div id="signin-panel" hidden><h3 id="signin-title">Sign in</h3><p id="signin-instructions"></p><div id="signin-password-fields" hidden><div class="row"><label for="signin-username">Username</label><input id="signin-username" autocomplete="username"></div><div class="row"><label for="signin-password">Password</label><input type="password" id="signin-password" autocomplete="current-password"></div></div><button type="button" id="signin-button">Sign in</button><button type="button" id="signin-cancel">Cancel</button><p id="signin-result" role="status" aria-live="polite"></p><p id="signin-open-link" hidden></p></div></section>`; }
export function servicePanel() { return `<section id="services-section" aria-labelledby="h-services"><h2 id="h-services">Spotify and hosted card</h2><p>These are optional. Spotify appears on your card but not Discord. Card hosting sends only the fields your privacy settings allow.</p><div><h3>Spotify</h3><p id="spotify-account">Not connected</p><label for="spotify-client-id">Your Spotify Client ID</label> <input id="spotify-client-id" autocomplete="off" placeholder="32-character Client ID"><p class="hint">Create your own Spotify app and register http://127.0.0.1/spotify/callback as a redirect URI.</p><button type="button" id="spotify-connect">Sign in with Spotify</button> <button type="button" id="spotify-remove" hidden>Disconnect Spotify</button><p id="spotify-service-result" role="status" aria-live="polite"></p><p id="spotify-open-link" hidden></p></div><div><h3>Hosted card</h3><p id="hosted-account">Not signed in</p><label for="hosted-address">Card service address</label> <input id="hosted-address" type="url" value="https://nowplaying-hosted.vercel.app" autocomplete="off"><p><button type="button" id="hosted-check">Check service</button> <button type="button" id="hosted-connect">Sign in with GitHub</button></p><p id="hosted-service-result" role="status" aria-live="polite"></p><details id="hosted-preview"><summary>See what a hosted card would send</summary><ul id="hosted-sent"></ul><p id="hosted-withheld"></p></details></div></section>`; }
export const SERVER_CSS = `#discovered-list li,#servers-list li{display:flex;gap:8px;align-items:center;justify-content:space-between;padding:6px 0;border-bottom:1px solid #ddd;overflow-wrap:anywhere}#discovered-list button,#servers-list button{flex-shrink:0}details{margin:16px 0}summary{cursor:pointer;font-weight:600}input[type=url],input[type=password],input:not([type]){font:inherit;max-width:100%;padding:5px 8px;border:1px solid #888;border-radius:6px;color:inherit;background:transparent}#signin-panel{border-top:1px solid #ddd;padding-top:12px}#services-section>div{border-top:1px solid #ddd;margin-top:16px;padding-top:8px}#spotify-client-id,#hosted-address{max-width:100%;box-sizing:border-box;width:320px}#signin-result{overflow-wrap:anywhere}@media(prefers-color-scheme:dark){#discovered-list li,#servers-list li,#signin-panel{border-color:#444}}`;
export const SERVER_SCRIPT = `"use strict";
(() => {
const $ = (id) => document.getElementById(id);
if (!$('servers-section')) return;
let selection = null, flow = null, timer = null, cancelRequested = false, scanRunning = false, returnFocus = null;
const label = { plex: 'Plex', jellyfin: 'Jellyfin', emby: 'Emby', navidrome: 'Navidrome' };
const say = (id, text) => { $(id).textContent = text; };
function li(text, button, action) { const row = document.createElement('li'); const span = document.createElement('span'); span.textContent = text; row.append(span); if (button) { const b = document.createElement('button'); b.type='button'; b.textContent=button; b.addEventListener('click', action); row.append(b); } return row; }
async function request(path, method='GET', data) { const r = await fetch(path, { method, cache:'no-store', headers:{Accept:'application/json',...(method==='GET'?{}:{'Content-Type':'application/json'})}, ...(data?{body:JSON.stringify(data)}:{...(method==='DELETE'?{body:'{}'}:{})}) }); const body = await r.json().catch(()=>({})); if (!r.ok) throw new Error(({too_many_servers:'You can add up to 8 servers.',invalid_server_url:'Enter a server address before signing in.',draft_update_failed:'Could not save the server. Try again.',credential_store_failed:'Could not save or restore the sign-in. Check the saved sign-in before trying again.'})[body.error] || body.error || 'request_failed'); return body; }
async function load() { try { const state = await request('/api/settings/servers'); const list = $('servers-list'); list.replaceChildren(...(state.servers.length ? state.servers.map((server) => li((label[server.provider]||server.provider)+' - '+server.name+' - '+server.baseUrl, state.servers.length>1?'Remove':null, async () => { if (!confirm('Remove this server?')) return; try { await request('/api/settings/servers','DELETE',{provider:server.provider,id:server.id}); await load(); } catch (e) { say('discovery-state','Could not remove: '+e.message); } })) : [li('No server connected yet.') ])); if ($('first-run-state')) { if (state.activationFailed) activationError(); else if (state.configured) waitForActivation(); else say('first-run-state','Choose a discovered server or add one by address to get started.'); } } catch { say('discovery-state','Could not load servers. Reload this page.'); } }
let activating=false, activationStopped=false;
function activationError() { if (activationStopped) return; activationStopped=true; say('first-run-state',"NowPlaying couldn't start after saving the server."); $('activation-recovery').hidden=false; }
function waitForActivation() { if (activating || activationStopped) return; activating=true; say('first-run-state','Server saved. Starting NowPlaying...'); const deadline=Date.now()+30000; setTimeout(activationError,30000); const check=async()=>{ if (activationStopped) return; try { const r=await fetch('/api/status',{cache:'no-store'}); if (activationStopped) return; if (r.ok) { location.assign('/settings'); return; } const state=await request('/api/settings/servers'); if (activationStopped) return; if (state.activationFailed) { activationError(); return; } } catch {} if (Date.now() >= deadline) { activationError(); return; } setTimeout(check,1200); }; setTimeout(check,1200); }
function showAuthLink(id, url) { const out=$(id); out.replaceChildren(); out.hidden=!url; if (!url) return; const a=document.createElement('a'); a.href=url; a.target='_blank'; a.rel='noopener noreferrer'; a.textContent=id==='signin-open-link'?'Open Plex sign-in':'Open sign-in page'; out.replaceChildren(a); }
function stopFlow() { if (timer) clearTimeout(timer); timer=null; if (flow) request('/api/setup/signin','POST',{action:'cancel',flowId:flow}).catch(()=>{}); flow=null; $('signin-password').value=''; }
function choose(provider, baseUrl, trigger) { stopFlow(); showAuthLink('signin-open-link', null); returnFocus=trigger; selection={provider,baseUrl}; $('server-provider').value=provider; $('server-url').value=baseUrl; $('signin-panel').hidden=false; $('signin-title').textContent='Connect to '+(label[provider]||provider); $('signin-password-fields').hidden=!['emby','navidrome'].includes(provider); $('signin-instructions').textContent=provider==='plex'?'Sign in on the Plex page that opens.':provider==='jellyfin'?'Approve the Quick Connect code in your Jellyfin dashboard.':'Sign in as the user whose playback you want to show. Your password is not saved.'; $('signin-button').textContent=['emby','navidrome'].includes(provider)?'Sign in':'Start sign-in'; say('signin-result',''); $('signin-button').focus(); }
async function finish(result) { if (result.status==='signed_in') { showAuthLink('signin-open-link',null); flow=null; say('signin-result','Connected as '+result.identity.displayName+'. NowPlaying is applying this change.'); $('signin-panel').hidden=true; await load(); if ($('first-run-state')) waitForActivation(); return; } if (result.status==='pending' && flow) { timer=setTimeout(async()=>{ try { await finish(await request('/api/setup/signin','POST',{action:'poll',flowId:flow})); } catch(e) { flow=null; say('signin-result','Sign-in failed: '+e.message); } },2000); } }
$('discover-servers').addEventListener('click', async()=>{ $('discover-servers').disabled=true; $('cancel-discovery').hidden=false; cancelRequested=false; scanRunning=true; $('discovered-list').replaceChildren(); say('discovery-state','Scanning. This may take a few seconds.'); try { const result=await request('/api/settings/servers/discover','POST',{subnet:$('scan-subnet').value.trim()}); if (result.cancelled) { say('discovery-state','Scan cancelled. Not all addresses were checked.'); return; } const servers=result.servers||[]; $('discovered-list').replaceChildren(...servers.map((s)=>li((label[s.provider]||s.provider)+' - '+(s.name ? s.name+' - ':'')+s.baseUrl,'Connect',(event)=>choose(s.provider,s.baseUrl,event.currentTarget)))); say('discovery-state',servers.length ? servers.length+' server(s) found.' : 'No servers found. Add one by address below.'); } catch(e) { say('discovery-state',e.message==='invalid_subnet'?'Enter a private subnet like 192.168.1.0/24.':e.message==='request_failed'?'Scan did not finish. Try again.':e.message); } finally { scanRunning=false; $('discover-servers').disabled=false; $('cancel-discovery').hidden=true; $('cancel-discovery').disabled=false; } });
$('cancel-discovery').addEventListener('click',async()=>{ if (!scanRunning || cancelRequested) return; cancelRequested=true; $('cancel-discovery').disabled=true; say('discovery-state','Cancelling scan...'); try { const result=await request('/api/settings/servers/cancel','POST'); if (result.cancelled) { if (scanRunning) say('discovery-state','Cancelling scan...'); } else if (scanRunning) say('discovery-state','Scan finished before cancellation. Waiting for results...'); } catch { if (scanRunning) say('discovery-state','Could not cancel the scan. Waiting for results...'); } finally { cancelRequested=false; if (scanRunning) $('cancel-discovery').disabled=false; } });
$('manual-connect').addEventListener('click',(event)=>{ try { const u=new URL($('server-url').value); if (!['http:','https:'].includes(u.protocol)||u.username||u.password||u.search||u.hash) throw Error(); choose($('server-provider').value,u.toString().replace(/\\/+$/,''),event.currentTarget); } catch { say('discovery-state','Enter a full http:// or https:// server address without a username or password.'); } });
$('signin-cancel').addEventListener('click',()=>{ showAuthLink('signin-open-link',null); stopFlow(); $('signin-panel').hidden=true; (returnFocus?.isConnected ? returnFocus : $('discover-servers')).focus(); returnFocus=null; });
$('signin-button').addEventListener('click',async()=>{ if (!selection) return; $('signin-button').disabled=true; say('signin-result','Connecting...'); try { const { provider, baseUrl }=selection; if (['emby','navidrome'].includes(provider)) { await finish(await request('/api/setup/signin','POST',{action:'password',provider,baseUrl,username:$('signin-username').value,password:$('signin-password').value})); $('signin-password').value=''; } else { const r=await request('/api/setup/signin','POST',{action:'start',provider,baseUrl}); flow=r.flowId; if (provider==='plex' && /^https:\\/\\/app\\.plex\\.tv\\//.test(r.authUrl||'')) { showAuthLink('signin-open-link',r.authUrl); window.open(r.authUrl,'_blank','noopener'); } if (provider==='jellyfin') say('signin-result','Enter Quick Connect code '+r.code+' in Jellyfin.'); else say('signin-result', $('signin-open-link').hidden ? 'Plex sign-in link is unavailable. Cancel and try again.' : 'Use the sign-in link below if a tab did not open. This page updates when you finish.'); await finish(r); } } catch(e) { say('signin-result','Sign-in failed: '+e.message); } finally { $('signin-password').value=''; $('signin-button').disabled=false; } });
load();
})();`;
export const SERVICE_SCRIPT = `"use strict";
(() => {
const $ = (id) => document.getElementById(id); if (!$('services-section')) return;
let spotifyFlow=null, hostedFlow=false;
const say=(id,text)=>{ $(id).textContent=text; };
async function api(path,method='GET',body) { const r=await fetch(path,{method,cache:'no-store',headers:{Accept:'application/json',...(method==='POST'?{'Content-Type':'application/json'}:{})},...(body?{body:JSON.stringify(body)}:{})}); const d=await r.json().catch(()=>({})); if (!r.ok) throw Error(d.error||d.status||'request_failed'); return d; }
async function load() { try { const s=await api('/api/settings/services'); say('spotify-account',s.spotify ? 'Connected as '+s.spotify.name : 'Not connected'); $('spotify-remove').hidden=!s.spotify; if(s.spotify) $('spotify-client-id').value=s.spotify.clientId; say('hosted-account',s.hosted.login ? 'Signed in as '+s.hosted.login : 'Not signed in'); $('hosted-address').value=s.hosted.url; } catch { say('spotify-service-result','Could not load connected services.'); } }
$('spotify-connect').addEventListener('click',async()=>{ $('spotify-open-link').replaceChildren(); $('spotify-open-link').hidden=true; try { const d=await api('/api/setup/spotify','POST',{action:'start',clientId:$('spotify-client-id').value.trim()}); spotifyFlow=d.flowId; if(/^https:\\/\\/accounts\\.spotify\\.com\\//.test(d.authUrl||'')) { const out=$('spotify-open-link'); out.replaceChildren(); const a=document.createElement('a'); a.href=d.authUrl; a.target='_blank'; a.rel='noopener noreferrer'; a.textContent='Open Spotify sign-in'; out.replaceChildren(a); out.hidden=false; window.open(d.authUrl,'_blank','noopener'); } say('spotify-service-result', $('spotify-open-link').hidden ? 'Spotify sign-in link is unavailable. Try again.' : 'Use the link below if a Spotify tab did not open.'); pollSpotify(); } catch(e) { say('spotify-service-result','Spotify sign-in could not start: '+e.message); } });
async function pollSpotify(){ if(!spotifyFlow)return; try { const d=await api('/api/setup/spotify','POST',{action:'poll',flowId:spotifyFlow}); if(d.status==='pending'){setTimeout(pollSpotify,2000);return;} spotifyFlow=null; $('spotify-open-link').replaceChildren(); $('spotify-open-link').hidden=true; say('spotify-service-result','Spotify connected.'); await load(); } catch(e){spotifyFlow=null;say('spotify-service-result','Spotify sign-in failed: '+e.message);} }
$('spotify-remove').addEventListener('click',async()=>{if(!confirm('Disconnect Spotify from this card?'))return;try{const d=await api('/api/settings/services','POST',{action:'remove-spotify'});say('spotify-service-result',d.tokenRemoved ? 'Spotify disconnected and the saved token was removed.' : 'Spotify disconnected from the card, but the saved token could not be deleted from this computer. Remove the nowplaying Spotify credential in your operating system credential manager.');await load();}catch(e){say('spotify-service-result','Could not disconnect Spotify: '+e.message);} });
$('hosted-preview').addEventListener('toggle',async(e)=>{if(!e.target.open)return;try{const p=await api('/api/setup/hosted/preview'); $('hosted-sent').replaceChildren(...p.sent.map((x)=>{const li=document.createElement('li');li.textContent=x.label;return li;}));say('hosted-withheld','Always sent: '+p.alwaysSent.join(', ')+'. Not sent: '+p.withheld.map(x=>x.label).concat(p.neverSent).join(', '));}catch{say('hosted-withheld','Preview unavailable.');}});
$('hosted-check').addEventListener('click',async()=>{try{const d=await api('/api/setup/hosted/check','POST',{url:$('hosted-address').value.trim()});say('hosted-service-result',d.ok?'NowPlaying card service is reachable.':'Service check failed: '+d.reason);}catch(e){say('hosted-service-result','Service check failed: '+e.message);} });
$('hosted-connect').addEventListener('click',async()=>{try{const d=await api('/api/setup/hosted/signin','POST',{action:'start',url:$('hosted-address').value.trim()});if(d.status!=='started')throw Error(d.status);hostedFlow=true;const a=document.createElement('a');a.href=d.verificationUri;a.target='_blank';a.rel='noopener';a.textContent=d.verificationUri;const out=$('hosted-service-result');out.replaceChildren('Open ',a,' and enter code '+d.userCode+'.');setTimeout(pollHosted,Math.max(5,Number(d.interval)||5)*1000);}catch(e){say('hosted-service-result','GitHub sign-in could not start: '+e.message);} });
async function pollHosted(){if(!hostedFlow)return;try{const d=await api('/api/setup/hosted/signin','POST',{action:'poll'});if(d.status==='pending'){setTimeout(pollHosted,5000);return;}hostedFlow=false;if(d.status!=='signed_in')throw Error(d.status);say('hosted-service-result','Signed in. Your card link: '+d.cardUrl);await load();}catch(e){hostedFlow=false;say('hosted-service-result','GitHub sign-in failed: '+e.message);} }
load();
})();`;
